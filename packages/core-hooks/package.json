{
  "name": "@boxfox/core-hooks",
  "version": "1.0.14",
  "description": "core hooks",
  "types": "dist/index.d.ts",
  "main": "dist/index.js",
  "module": "dist/esm",
  "scripts": {
    "build": "tsc -p tsconfig.json && tsc -p tsconfig.esm.json",
    "deploy": "yarn build && yarn publish"
  },
  "devDependencies": {
    "@types/react": "^17.0.3",
    "react": "^17.0.2",
    "throttle": "^1.0.3",
    "typescript": "^4.2.4"
  },
  "peerDependencies": {
    "react": ">=17.0.2",
    "throttle": ">=1.0.3"
  },
  "publishConfig": {
    "registry": "https://boxfox.bytesafe.dev/r/boxfox/"
  }
}
