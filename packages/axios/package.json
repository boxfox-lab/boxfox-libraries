{
  "name": "@boxfox/axios",
  "version": "0.0.7",
  "description": "axios libraries",
  "types": "dist/index.d.ts",
  "main": "dist/index.js",
  "module": "dist/esm",
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json && tsc -p tsconfig.esm.json",
    "deploy": "yarn build && yarn publish"
  },
  "peerDependencies": {
    "axios": ">=0.21.1"
  },
  "devDependencies": {
    "axios": "^0.21.1",
    "typescript": "^4.6.2"
  },
  "publishConfig": {
    "registry": "https://boxfox.bytesafe.dev/r/boxfox/"
  }
}
